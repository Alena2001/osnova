#include <stdio.h>
#include <string.h>
int main()
{
    int i, k=0;
    char c='a';
    char a[16];
    scanf("%s", &a);
    for (i=0; a[i]!=0; i++)
    {
        if (a[i]==c){
            k+=1;
        }
    }
    printf("%d", k);

    return 0;
}
